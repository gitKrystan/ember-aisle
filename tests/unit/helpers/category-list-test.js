import { categoryList } from 'aisle/helpers/category-list';
import { module, test } from 'qunit';

module('Unit | Helper | category list');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = categoryList([42]);
  assert.ok(result);
});
